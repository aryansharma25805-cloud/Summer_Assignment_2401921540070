class Solution {
public:
    int firstUniqChar(string s) {
        vector<int> out(26, 0);
        for (char c : s)
            out[c - 'a']++;
        for (int i = 0; i < s.size(); i++) {
            if (out[s[i] - 'a'] == 1)
                return i;
        }
        return -1;
    }
};